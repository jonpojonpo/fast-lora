# Core dependencies
torch>=2.0.0
transformers>=4.30.0
peft>=0.4.0
accelerate>=0.23.0  # Corrected from accelerata
bitsandbytes>=0.41.0
datasets>=2.12.0
huggingface_hub>=0.16.0

# For GGUF conversion
llama-cpp-python>=0.1.77
# NOTE: If the llama_cpp.model_converter module is not available, the script will use
# a fallback method to create a test GGUF file. For production use, you would need 
# to ensure proper GGUF conversion tools are installed.
pydantic>=2.3.0

# Utility libraries
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# API clients (optional, comment out if not needed)
# openai>=0.27.0
# anthropic>=0.5.0
